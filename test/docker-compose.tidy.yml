services:
  clickhouse-dash:
    build:
        context: ../
        dockerfile: ./test/Dockerfile
        target: base
    command: ["go", "mod", "tidy"]
    container_name: clickhouse-tidy
    restart: no
    volumes:
      - ../src:/src/:rw